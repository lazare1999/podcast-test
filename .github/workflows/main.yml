name: Generate podcast feed
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Run Feed Generator
        uses: lazare1999/podcast_generator@main